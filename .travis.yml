language: ruby
rvm:
  - 2.1.1
cache:
  - bundler
  - apt
bundler_args: --quiet
branches:
  only:
    - master
    - develop
before_script:
  - bundle exec rake db:create db:schema:load
script:
  - export RAILS_ENV=test
  - bundle exec rspec
